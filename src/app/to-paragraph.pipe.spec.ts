import { ToParagraphPipe } from './to-paragraph.pipe';

describe('ToParagraphPipe', () => {
  it('create an instance', () => {
    const pipe = new ToParagraphPipe();
    expect(pipe).toBeTruthy();
  });
});
